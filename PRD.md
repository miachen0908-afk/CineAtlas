# 世界电影数字地图：简化版 PRD

## 1. 项目目标

开发一个以3D世界地图为核心的沉浸式电影发现网站。

用户可以：

- 在3D世界地图上探索不同国家的电影。
- 通过年份滑杆查看某一年上映的影片。
- 点击地图上的星光查看影片。
- 按国家和电影类型筛选影片。
- 查看影片详情并点喜欢。
- 通过人物时间轴了解世界电影史中的代表人物。

目标用户为电影爱好者和电影相关专业学生。

本项目首先制作前端MVP，用少量本地示例数据验证主要体验。暂不开发真实后端。

---

## 2. 技术要求

使用以下技术：

- Next.js
- TypeScript
- Tailwind CSS
- React Three Fiber
- Three.js
- Zustand
- Framer Motion

开发要求：

- 使用TypeScript，避免使用`any`。
- 使用响应式布局，支持桌面端和移动端。
- 数据、状态和UI组件相互分离。
- 不添加后端、登录系统和数据库。
- 用户喜欢数据使用浏览器本地存储。
- 不使用未经授权的商业电影海报。
- 不实现PRD范围之外的功能。

---

## 3. MVP范围

首版使用本地JSON数据，包含：

- 6个国家。
- 约30部影片。
- 约12位代表电影人物。
- 6个基础电影类型。
- 年份范围为1920—2025。

建议使用的国家：

- 中国
- 日本
- 法国
- 意大利
- 美国
- 印度

首版需要完成：

1. 3D世界地图。
2. 影片星光。
3. 年份滑杆。
4. 国家和类型筛选。
5. 影片预览卡片。
6. 影片详情页。
7. 国家电影页面。
8. 世界电影人物时间轴。
9. 本地喜欢功能。
10. 基础响应式适配。

首版不开发：

- 用户登录和注册。
- 评论及留言。
- 真实评分系统。
- 后台管理系统。
- 推荐算法。
- 多人社交功能。
- 人物关系网络。
- 历史国界变化。
- 多国时间轴对比。
- 完整海报模式。
- 背景音乐。
- 真实商业电影海报。

---

## 4. 影片归属规则

影片在地图中的国家归属遵循以下规则：

1. 优先使用第一出品国家。
2. 合拍片只归入第一出品国家。
3. 缺少出品国家信息时，使用导演主要国籍。
4. 其他出品国家只在影片详情中展示。
5. 有明确出品城市时，将影片放在该城市。
6. 没有城市信息时，将影片放在所属国家的默认坐标。
7. 拍摄地点作为补充资料，不影响影片主要归属。

当前版本使用现代国家边界，不处理历史国界变化。

---

## 5. 页面结构

需要创建以下页面：

```text
/
世界电影地图首页

/country/[countryCode]
国家电影页面

/film/[filmId]
影片详情页

/people
世界电影人物时间轴
```

---

## 6. 世界地图首页

首页是产品的核心页面。

### 页面布局

顶部：

- 产品名称。
- 搜索占位入口。
- “世界电影群像”入口。

左侧控制区：

- 类型筛选。
- 国家筛选。
- 清除筛选按钮。

中间：

- 3D世界地图。
- 影片星光。

右侧：

- 用户点击影片后出现的影片预览卡片。

底部：

- 年份滑杆。
- 当前年份。

移动端：

- 控制区使用抽屉或弹层。
- 影片预览使用底部卡片。
- 支持单指旋转和双指缩放。

---

## 7. 3D地图

### 基础要求

- 显示可旋转的3D地球。
- 使用深色背景和电影星空风格。
- 显示基本国家边界。
- 支持鼠标拖动。
- 支持滚轮缩放。
- 支持移动端触摸操作。
- 限制最大和最小缩放距离。
- 镜头不能进入地球内部。

3D效果主要用于营造沉浸氛围，不需要复杂的地理模拟。

### 国家交互

- 点击国家后高亮该国家。
- 镜头平滑移动至被选国家。
- 显示国家名称和影片数量。
- 提供进入国家详情页的按钮。
- 支持取消选择并返回世界视角。

---

## 8. 影片星光

- 地图上的一个星光点代表一部影片。
- 星光位置来自影片的经纬度。
- 同一区域星光越多，表示影片数量越多。
- 只显示符合当前年份和筛选条件的影片。
- 点击星光后显示影片预览卡片。
- 选中的星光需要高亮。
- 同一城市多部影片可以进行轻微位置偏移，避免完全重叠。
- 当前版本不使用星光亮度表示评分或喜欢数量。
- 当前版本不使用不同颜色区分电影类型。

需要将经纬度转换为3D球体坐标，并将转换逻辑放在独立工具函数中。

---

## 9. 年份滑杆

- 年份范围：1920—2025。
- 用户一次选择一个年份。
- 清晰显示当前年份。
- 选择年份后，只显示该年影片。
- 年份变化时，星光平滑进入或离开。
- 快速拖动时不能造成明显卡顿。
- 没有影片的年份需要显示空状态提示。

当前版本不支持年份区间。

---

## 10. 筛选功能

首版支持：

- 按国家筛选。
- 按单个类型筛选。
- 年份、国家和类型可以组合使用。
- 支持一键清除筛选。
- 页面显示当前生效的筛选条件。
- 没有结果时显示明确提示。

建议基础类型：

- 剧情
- 喜剧
- 爱情
- 动作
- 恐怖
- 动画

---

## 11. 影片预览卡片

点击星光后显示影片预览卡片。

需要包含：

- 占位海报或电影色块。
- 中文片名。
- 原文片名。
- 年份。
- 第一出品国家。
- 导演。
- 类型。
- 简短简介。
- 模拟评分。
- 喜欢按钮。
- 查看详情按钮。

交互要求：

- 点击其他星光时更新卡片内容。
- 点击地图空白区域时关闭卡片。
- 桌面端使用右侧浮层。
- 移动端使用底部卡片。

---

## 12. 影片详情页

影片详情页需要显示：

- 中文片名。
- 原文片名。
- 占位海报。
- 上映年份。
- 第一出品国家。
- 其他出品国家。
- 出品城市。
- 拍摄地点。
- 导演。
- 类型。
- 影片简介。
- 模拟评分。
- 喜欢数量。
- 喜欢按钮。
- 返回地图按钮。

返回地图时尽可能保留：

- 选择的年份。
- 国家筛选。
- 类型筛选。
- 当前地图视角。

---

## 13. 喜欢功能

首版不需要登录。

- 用户可以在影片卡片和详情页点喜欢。
- 再次点击可以取消喜欢。
- 两个页面中的喜欢状态必须同步。
- 喜欢过的影片ID保存在浏览器本地存储中。
- 页面刷新后喜欢状态仍然保留。
- 当前版本不制作喜好热度地图。

---

## 14. 国家电影页面

国家页面需要显示：

- 国家名称。
- 国家电影简介。
- 当前选择年份。
- 当前年份的影片数量。
- 代表影片。
- 影片列表。
- 代表人物。
- 主要电影城市。
- 类型分布。
- 返回世界地图按钮。

要求：

- 内容来自统一的本地数据。
- 点击影片进入影片详情。
- 点击人物显示人物详情。
- 没有内容时显示空状态。
- 当前版本不制作多国时间轴对比。

---

## 15. 世界电影人物时间轴

模块名称暂定为“世界电影群像”。

这是从早期电影到当代的重要人物时间轴，不是单个人物的个人年谱。

### 页面要求

- 使用横向时间轴。
- 显示年份刻度。
- 显示约12位代表人物。
- 人物使用统一风格的简笔画占位形象。
- 支持横向拖动浏览。
- 支持按国家筛选。
- 支持按职业筛选。
- 点击人物后显示人物信息。

### 人物信息

- 中文姓名。
- 原文姓名。
- 国家。
- 职业。
- 出生与逝世年份。
- 简短介绍。
- 代表作品。
- 喜欢数量。

人物在时间轴上的年份由数据中的`timelineYear`字段直接指定，不自动计算。

首版人物以导演和演员为主。

---

## 16. 数据结构

数据使用本地JSON文件：

```text
data/
├── countries.json
├── films.json
├── people.json
└── genres.json
```

### 影片类型

```ts
type Film = {
  id: string
  titleZh: string
  titleOriginal: string
  year: number
  primaryProductionCountry: string
  otherProductionCountries: string[]
  productionCity?: string
  filmingLocations: {
    name: string
    latitude?: number
    longitude?: number
  }[]
  directorIds: string[]
  genreIds: string[]
  posterUrl?: string
  summary: string
  rating?: number
  likeCount: number
  coordinates: {
    latitude: number
    longitude: number
    accuracy: "city" | "country"
  }
}
```

### 人物类型

```ts
type Person = {
  id: string
  nameZh: string
  nameOriginal: string
  birthYear: number
  deathYear?: number
  countryCodes: string[]
  profession: string[]
  timelineYear: number
  summary: string
  representativeFilmIds: string[]
  portraitUrl?: string
  likeCount: number
}
```

### 国家类型

```ts
type Country = {
  code: string
  nameZh: string
  nameEn: string
  center: {
    latitude: number
    longitude: number
  }
  summary: string
}
```

所有数据必须使用唯一ID关联，不在组件中重复写死影片和人物信息。

---

## 17. 状态管理

使用Zustand统一管理：

```ts
type MapState = {
  selectedYear: number
  selectedCountryCode: string | null
  selectedGenreId: string | null
  selectedFilmId: string | null
  likedFilmIds: string[]
}
```

需要提供对应的设置、清除和喜欢操作。

地图组件、筛选组件和影片卡片必须读取同一份状态，不能各自保存重复状态。

---

## 18. 视觉方向

关键词：

- 深色
- 宇宙
- 星光
- 银幕
- 时间
- 世界
- 沉浸
- 克制
- 档案感

要求：

- 整体使用深色背景。
- 星光是地图的主要视觉元素。
- 国家边界清晰但不能过度突出。
- 卡片可以使用半透明效果。
- 页面动画保持平缓。
- 不使用过多霓虹色。
- 重要信息不能只通过颜色表达。
- 支持系统“减少动态效果”设置。

---

## 19. 推荐目录结构

```text
app/
├── page.tsx
├── country/[countryCode]/page.tsx
├── film/[filmId]/page.tsx
├── people/page.tsx
└── layout.tsx

components/
├── globe/
│   ├── CinemaGlobe.tsx
│   ├── CountryLayer.tsx
│   ├── FilmStars.tsx
│   └── GlobeControls.tsx
├── timeline/
│   ├── YearSlider.tsx
│   └── PeopleTimeline.tsx
├── film/
│   ├── FilmPreviewCard.tsx
│   ├── FilmDetail.tsx
│   └── LikeButton.tsx
├── filters/
│   ├── CountryFilter.tsx
│   └── GenreFilter.tsx
└── layout/
    ├── Header.tsx
    ├── LoadingScreen.tsx
    └── EmptyState.tsx

data/
├── countries.json
├── films.json
├── people.json
└── genres.json

store/
└── useMapStore.ts

types/
└── cinema.ts

utils/
├── geo.ts
└── filmFilters.ts

public/
├── posters/
├── people/
└── textures/
```

目录可以根据实际实现略微调整，但必须保持数据、状态、地图和普通UI组件分离。

---

## 20. 开发顺序

严格按照以下顺序推进：

1. 创建项目和页面骨架。
2. 建立TypeScript数据类型。
3. 创建本地示例数据。
4. 实现基础3D地球。
5. 实现经纬度到球面坐标转换。
6. 在地球上显示影片星光。
7. 实现年份滑杆。
8. 实现国家和类型筛选。
9. 实现影片预览卡片。
10. 实现影片详情页。
11. 实现本地喜欢功能。
12. 实现国家电影页面。
13. 实现世界电影人物时间轴。
14. 完成移动端适配。
15. 测试、修复和性能优化。

每一步完成并验证后，再开始下一步。

---

## 21. 验收标准

用户需要能够完成以下操作：

1. 打开网站并看见3D世界地图。
2. 拖动和缩放地图。
3. 通过年份滑杆选择年份。
4. 看见该年份对应的影片星光。
5. 按国家或类型筛选影片。
6. 点击星光查看影片预览。
7. 从预览进入影片详情。
8. 点喜欢并在刷新后保留状态。
9. 进入国家页面查看影片和人物。
10. 进入世界电影群像浏览代表人物。
11. 在桌面端和移动端完成主要操作。

同时满足：

- 页面无明显运行错误。
- TypeScript检查通过。
- 主要页面没有明显布局溢出。
- 筛选结果与本地数据一致。
- 影片位置与所属国家基本一致。
- 不使用未经授权的商业海报。
- 不包含PRD之外的额外系统。

---

## 22. Cursor执行规则

开始开发前，请先：

1. 阅读完整PRD。
2. 检查当前项目已有文件。
3. 建立`TASKS.md`。
4. 把开发任务拆成可验证的小步骤。
5. 说明第一个步骤准备修改哪些文件。

每次只完成一个阶段。完成后必须：

- 运行TypeScript检查。
- 运行项目构建或相关检查。
- 说明修改的文件。
- 说明实现结果。
- 提供人工验收方法。
- 更新`TASKS.md`。
- 等待确认后再扩展下一个阶段。

不要一次性生成整个项目，不要擅自增加后端、登录、评论、推荐或其他超出首版范围的功能。
